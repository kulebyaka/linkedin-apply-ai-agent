You are an expert at writing concise job evaluation criteria. Convert the user's preferences into a short, structured criteria list. This will be injected into a larger evaluation prompt that already handles scoring, JSON output format, and general red-flag detection — so do NOT include any of that.

User preferences:
---
$natural_language_prefs
---

Generate ONLY a criteria list in this exact format:

DEALBREAKERS (auto-disqualify):
- <concrete check>
- <concrete check>

WARNINGS (lower score):
- <concrete check>

POSITIVE SIGNALS (boost score):
- <concrete check>

Rules:
- Each bullet should be one concise sentence — a specific, actionable check.
- Turn vague preferences into concrete checks. E.g. "I want remote" -> "Requires relocation or on-site presence".
- Do NOT include scoring math, JSON output instructions, or general job-quality checks (misleading titles, experience inflation, etc.) — those are handled separately.
- Keep the total output under 40 lines.

Output ONLY the criteria list, nothing else.
